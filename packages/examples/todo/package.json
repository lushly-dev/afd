{
	"name": "@afd/example-todo",
	"version": "1.0.0",
	"private": true,
	"type": "module",
	"description": "Multi-stack Todo example with AFD patterns",
	"scripts": {
		"test:conformance:ts": "pnpm --dir backends/typescript build && tsx dx/run-conformance.ts --backend ts",
		"test:conformance:py": "tsx dx/run-conformance.ts --backend py",
		"test:conformance:rs": "cargo build --locked --manifest-path backends/rust/Cargo.toml && tsx dx/run-conformance.ts --backend rs",
		"typecheck": "tsc -p tsconfig.typecheck.json",
		"dev:ts": "cd backends/typescript && pnpm dev",
		"dev:py": "uv run --project backends/python todo-server",
		"dev:web": "cd frontends/vanilla && npx serve .",
		"dev:react": "cd frontends/react && pnpm dev"
	},
	"devDependencies": {
		"@modelcontextprotocol/sdk": "^1.0.1",
		"ts-node": "^10.9.2",
		"tsx": "^4.23.15",
		"typescript": "^5.3.3"
	}
}
