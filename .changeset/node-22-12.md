---
'@lushly-dev/afd-core': major
---

All `@lushly-dev/*` packages now require Node.js 22.12 or later (`engines.node` was `>=22.0.0`). The client's `eventsource` 5 dependency declares Node.js 22.12, and every package ships at the same version, so the whole family uses the same minimum.
