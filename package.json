{
	"name": "afd",
	"version": "0.3.4",
	"private": true,
	"description": "Agent-First Development - Build software where AI agents are first-class users",
	"repository": {
		"type": "git",
		"url": "https://github.com/lushly-dev/afd.git"
	},
	"author": "Falkicon",
	"license": "MIT",
	"type": "module",
	"engines": {
		"node": ">=20.0.0",
		"pnpm": ">=9.0.0"
	},
	"packageManager": "pnpm@10.30.1",
	"scripts": {
		"build": "pnpm -r build",
		"clean": "pnpm -r clean && rimraf node_modules",
		"dev": "pnpm -r --parallel dev",
		"lint": "biome check .",
		"lint:fix": "biome check --write .",
		"format": "biome format --write .",
		"test": "pnpm -r test",
		"test:coverage": "pnpm -r --no-bail test -- --coverage",
		"test:watch": "pnpm -r --parallel test:watch",
		"typecheck": "pnpm -r typecheck",
		"check": "npx lefthook run check",
		"release": "node scripts/release.mjs",
		"publish:npm": "pnpm -r --filter '@lushly-dev/*' publish --no-git-checks --access public",
		"prepare": "lefthook install"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.4",
		"@commitlint/cli": "^19.8.1",
		"@commitlint/config-conventional": "^19.8.1",
		"@types/node": "^25.3.0",
		"@vitest/coverage-v8": "^4.0.18",
		"lefthook": "^2.1.1",
		"rimraf": "^6.1.3",
		"typescript": "^5.9.3",
		"vitest": "^4.0.18"
	}
}
